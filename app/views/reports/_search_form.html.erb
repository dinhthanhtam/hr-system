  <table class="formTbl contents">
    <tbody>
      <tr>
        <th><%= f.label :report_category %></th>
        <td><%= f.collection_select :report_category_id_eq, ReportCategory.all, :id, :name, include_blank: true %></td>
      </tr>
      <tr>
        <th><%= f.label :title %></th>
        <td><%= f.text_field :title_cont %></td>
      </tr>
      <tr>
        <th><%= f.label :description %></th>
        <td><%= f.text_field :description_cont %></td>
      </tr>
      <tr>
        <th><%= f.label :report_date %></th>
        <td>
          <%= f.text_field :report_date_gteq, class: "datepicker" %>
          <%= f.text_field :report_date_lteq, class: "datepicker" %>
        </td>
      </tr>
      <tr>
        <th><%= f.label :month %></th>
        <td>
          <%= f.select :month_eq, (1..12).to_a, include_blank: true %>
        </td>
      </tr>
      <tr>
        <th><%= f.label :year %></th>
        <td>
          <%= f.select :year_eq, (2012..Time.now.year).to_a, include_blank: true %>
        </td>
      </tr>
    </tbody>
  </table>
  
  <%= hidden_field_tag :member_id, params[:member_id]%>

