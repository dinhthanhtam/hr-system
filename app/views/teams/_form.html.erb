<%= form_for @team, url: {action: @team.new_record? ? :create : :update} do |f| %>

  <%= render partial: "errors", locals: {object: @team} %>

  <table class="formTbl contents">
    <tr>
      <th><%= f.label :name %></th>
      <td><%= f.text_field :name %></td>
    </tr>
    
  </table>
  <div class="form-actions">
    <%= f.submit @team.new_record? ? "Create" : "Edit", class:"btn btn-primary" %>
  </div>
<% end %>
